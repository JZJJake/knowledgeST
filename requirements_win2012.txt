# Windows Server 2012 R2 & Python 3.11 Compatible Dependencies
# Enforce pre-compiled wheels to prevent local C++ compilation

fastapi==0.103.1
uvicorn==0.23.2
sqlalchemy==2.0.21
aiohttp==3.8.5
pydantic==2.7.0
openai==1.14.0
sentence-transformers==2.7.0

# ChromaDB 0.5.0+ migrated away from the problematic 'hnswlib' to 'chroma-hnswlib',
# which natively distributes pre-compiled Windows wheels (cp311-win_amd64) on PyPI.
# This strictly avoids local C++ compilation.
chromadb==0.5.0
